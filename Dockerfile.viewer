FROM node:18-alpine

WORKDIR /app

# Copy everything
COPY . .

# Install dependencies
RUN npm install --production

# Build if needed
RUN npm run build || true

# Expose port
EXPOSE 3000

# Start command
CMD ["npm", "start"]
